# Java Code Graph - Configuration
target_project: "F:/code/java/roncoo-pay"

# Package prefixes to include in the graph
scan_packages:
  - "com.roncoo.pay"

# Entry packages - auto-discover all Controller methods in these packages
entry_packages:
  - "com.roncoo.pay"

# Entry points (optional, can be auto-discovered from entry_packages)
# Format: fully.qualified.ClassName#methodName
# entry_points: []
